// Translation dictionary for Spanish (default), Portuguese and English
const translations = {
  es: {
    "nav.home": "Inicio",
    "nav.methodology": "Metodología",
    "nav.services": "Especialidades",
    "nav.cases": "Casos de Éxito",
    "nav.contact": "Contacto",
    
    "hero.subtitle": "EPYX SAS Uruguay",
    "hero.title": "Plataformas de Pago y Automatización con Inteligência Artificial",
    "hero.desc": "Concebimos y producimos desde Uruguay para el mundo. Squads hiper-especializadas integradas por desarrolladores de élite y programadores virtuales de última generación guiados bajo el concepto de Spec-First.",
    "hero.cta.simulate": "Simular Squad de IA",
    "hero.cta.contact": "Fale com um Especialista",
    
    "methodology.subtitle": "Metodología Core",
    "methodology.title": "Spec-First & Vibe Coding",
    "methodology.desc": "La nueva generación del desarrollo de software no se trata de escribir código a ciegas. Creamos sistemas hiper-eficientes estructurando especificaciones detalladas preparadas para que múltiples agentes autónomos colaboren y resuelvan problemas complejos.",
    "methodology.step1.title": "1. Especificación (Spec-First)",
    "methodology.step1.desc": "Diseñamos un plano de arquitectura exhaustivo. Una spec bien definida evita alucinaciones de IA y define interfaces rígidas antes de la codificación.",
    "methodology.step2.title": "2. Colaboración Multi-Agente",
    "methodology.step2.desc": "Desplegamos squads con agentes especializados (Antigravity, Claude Code, Cursor) integrados con arquitectos expertos para codificación en paralelo.",
    "methodology.step3.title": "3. Validación de Sistemas",
    "methodology.step3.desc": "Validadores autónomos ejecutan bancos de pruebas automáticas, garantizan tipos estrictos y auditan la seguridad antes de cualquier despliegue.",

    "simulator.subtitle": "Consola del Sistema",
    "simulator.title": "AI Squad Simulator",
    "simulator.label": "Seleccione un proyecto para ver los agentes cooperar:",
    "simulator.project.care": "Customer Care Multi-Agente",
    "simulator.project.docs": "Clasificación de Documentos Legales",
    "simulator.project.payments": "Pagos Multimoneda & Pix",
    "simulator.project.tokenization": "Tokenización de Activos & Cripto",
    "simulator.project.carbon": "Visualización Carbono & Energía",
    "simulator.project.agro": "Agro Embedded Financing",
    "simulator.terminal.tab.logs": "Logs del Sistema",
    "simulator.terminal.tab.chat": "Conversación IA",
    "simulator.terminal.tab.code": "Código Producido",
    "simulator.btn.run": "Ejecutar Simulación de Squad",
    "simulator.terminal.idle": "Consola inactiva. Seleccione un proyecto arriba y haga clic en 'Ejecutar Simulación'...",

    "services.subtitle": "Especialidades",
    "services.title": "Sistemas de Alta Complexidad",
    "services.card.care.title": "Sistemas Multi-Agente de Customer Care",
    "services.card.care.desc": "Flujos de atención al cliente integrados donde agentes especializados analizan sentimientos, escalan incidencias y resuelven consultas complejas automáticamente.",
    "services.card.docs.title": "Clasificación y Clusterización Legal",
    "services.card.docs.desc": "Análisis inteligente de contratos y documentos con validez jurídica. Indexación semántica, extracción de cláusulas y organización documental autónoma.",
    "services.card.payments.title": "Pagos Multimoneda & Pix Internacional",
    "services.card.payments.desc": "Pasarelas de pagos globales optimizadas desde Uruguay. Monederos multi-divisa, liquidación internacional y conversión instantánea.",
    "services.card.tokenization.title": "Tokenización de Activos & Capitalización",
    "services.card.tokenization.desc": "Tokenización de activos reales (RWA) y estructuración digital para la captación de capital de empresas tecnológicas y financieras con smart contracts.",
    "services.card.credit.title": "Financiamientos Respaldados en Cripto",
    "services.card.credit.desc": "Sistemas de crédito colateralizados en tokens integrando oráculos Chainlink y liquidaciones automáticas basadas en ratios de salud en tiempo real.",
    "services.card.apps.title": "Aplicaciones Fintech de Última Milla",
    "services.card.apps.desc": "Interfaces de usuario ultra-fluidas y seguras para monederos digitales, transferencias instantáneas y apps de pago móvil nativas.",
    "services.card.agro.title": "Agro Embedded Financing",
    "services.card.agro.desc": "Integración de pasarelas de crédito, scoring satelital y billeteras multi-moneda para cadenas de suministro agrícolas y emisión de CPRs.",
    "services.card.carbon.title": "Lead Generation & Emisiones de Carbono",
    "services.card.carbon.desc": "Análisis de datos, monitoreo de emisiones IoT y estructuración de proyectos de energía renovable respaldados en activos criptográficos.",

    "cases.subtitle": "Producido en Uruguay para el Mundo",
    "cases.title": "Casos Reales en Producción",
    "cases.tag.fintech": "Fintech & Blockchain",
    "cases.tag.dev": "Developer Tooling",
    "cases.tag.sustainability": "Sustentabilidad",
    "cases.card.wagari.desc": "Wagari Carbon es una plataforma verde diseñada para certificar, trazar y comercializar créditos de carbono globales con auditoría distribuida e inmutable.",
    "cases.card.urus.desc": "Urus Stack es una suite avanzada para desarrolladores que integra flujos automatizados de especificación-código-validación en entornos distribuidos.",
    "cases.card.paraguay.desc": "Portal Paraguai unifica el ecosistema de facturación electrónica y pasarela de cobros transfronterizos para empresas del Mercosur con alta resiliencia.",
    "cases.tag.agro": "Agro Embedded Finance",
    "cases.card.agro.desc": "Ceres Trading implementa una arquitectura de Agro Embedded Financing, integrando motores de scoring satelital y climático con pasarelas de crédito y derivativos agrícolas.",

    "contact.title": "Comience un Proyecto de Alta Tecnología",
    "contact.desc": "Diseñemos la especificación de su sistema y despleguemos una squad altamente capacitada de arquitectos de software y agentes virtuales.",
    "contact.form.name": "Nombre / Empresa",
    "contact.form.email": "Correo electrónico",
    "contact.form.message": "Cuéntenos sobre el sistema que deseja automatizar",
    "contact.form.submit": "Iniciar Solicitud de Proyecto",
    "contact.form.sending": "Estableciendo túnel seguro...",
    "contact.form.success": "Solicitud transmitida con éxito. Nuestro nodo de atención le responderá encriptadamente en breve."
  },
  pt: {
    "nav.home": "Início",
    "nav.methodology": "Metodologia",
    "nav.services": "Especialidades",
    "nav.cases": "Casos Reais",
    "nav.contact": "Contato",
    
    "hero.subtitle": "EPYX SAS Uruguay",
    "hero.title": "Plataformas de Pagamento e Automação com Inteligência Artificial",
    "hero.desc": "Concebidas e produzidas no Uruguai para o mundo. Squads altamente especializadas integrando programadores virtuais de última geração e profissionais experientes sob a metodologia Spec-First.",
    "hero.cta.simulate": "Simular Squad de IA",
    "hero.cta.contact": "Falar com um Especialista",
    
    "methodology.subtitle": "Metodologia Core",
    "methodology.title": "Spec-First & Vibe Coding",
    "methodology.desc": "A nova geração do desenvolvimento de software não consiste em codificar às cegas. Criamos sistemas excepcionais estruturando especificações completas criadas para guiar múltiplos agentes autônomos integrados a arquitetos humanos.",
    "methodology.step1.title": "1. Especificação (Spec-First)",
    "methodology.step1.desc": "Desenvolvemos especificações de arquitetura ultra-detalhadas que impedem alucinações de IA e definem interfaces bem delineadas antes do código.",
    "methodology.step2.title": "2. Squad de Agentes Virtuais",
    "methodology.step2.desc": "Ativamos programadores virtuais avançados como Antigravity, Claude Code e Cursor, operando com habilidades integradas de forma paralela.",
    "methodology.step3.title": "3. Validação Rigorosa",
    "methodology.step3.desc": "Agentes validadores autônomos executam testes automáticos de regressão, garantem tipagem estrita e testam a segurança da aplicação.",

    "simulator.subtitle": "Console do Sistema",
    "simulator.title": "AI Squad Simulator",
    "simulator.label": "Selecione um projeto para ver a squad de agentes colaborar:",
    "simulator.project.care": "Customer Care Multi-Agente",
    "simulator.project.docs": "Classificação de Documentos Legais",
    "simulator.project.payments": "Pagamentos Multi-moeda & Pix",
    "simulator.project.tokenization": "Tokenização de Ativos & Cripto",
    "simulator.project.carbon": "Visualização Carbono & Energia",
    "simulator.project.agro": "Agro Embedded Financing",
    "simulator.terminal.tab.logs": "Logs do Sistema",
    "simulator.terminal.tab.chat": "Conversa de Agentes",
    "simulator.terminal.tab.code": "Código Produzido",
    "simulator.btn.run": "Executar Simulação de Squad",
    "simulator.terminal.idle": "Console inativo. Selecione um projeto acima e clique em 'Executar Simulação'...",

    "services.subtitle": "Especialidades",
    "services.title": "Sistemas de Alta Complexidade",
    "services.card.care.title": "Sistemas Multi-Agente para Customer Care",
    "services.card.care.desc": "Atendimento automatizado inteligente onde agentes especializados analisam sentimentos, categorizam e respondem requisições complexas integrados a CRMs.",
    "services.card.docs.title": "Classificação e Clusterização Documental",
    "services.card.docs.desc": "Processamento avançado de linguagem natural para categorizar, indexar e agrupar contratos e documentos com valor jurídico de forma autônoma.",
    "services.card.payments.title": "Pagamentos Multi-Moedas & Pix Internacional",
    "services.card.payments.desc": "Plataformas robustas de checkout global. Wallets multi-currency, processamento Pix internacional e liquidação instantânea.",
    "services.card.tokenization.title": "Tokenização de Ativos & Capitalização",
    "services.card.tokenization.desc": "Tokenização de ativos do mundo real (RWA) e estruturação digital para captação de capital de empresas tecnológicas com smart contracts.",
    "services.card.credit.title": "Financiamento Lastreado em Cripto",
    "services.card.credit.desc": "Sistemas de crédito colateralizados em criptoativos, integrando oráculos Chainlink e liquidação automática por índices de liquidez.",
    "services.card.apps.title": "Aplicativos Fintech de Última Milha",
    "services.card.apps.desc": "Desenvolvimento de aplicativos de pagamento modernos com alta segurança, conformidade e experiência de usuário de nível bancário.",
    "services.card.agro.title": "Agro Embedded Financing",
    "services.card.agro.desc": "Integração de gateways de crédito, scoring satelital e carteiras multi-moeda para cadeias de suprimento agrícolas e emissão de CPRs.",
    "services.card.carbon.title": "Lead Generation & Emissões de Carbono",
    "services.card.carbon.desc": "Análise de dados, monitoramento de emissões IoT e estruturação de projetos de energia renovável lastreados em ativos criptográficos.",

    "cases.subtitle": "Concebido no Uruguai para o mundo",
    "cases.title": "Casos Reais em Produção",
    "cases.tag.fintech": "Fintech & Blockchain",
    "cases.tag.dev": "Developer Tooling",
    "cases.tag.sustainability": "Sustentabilidade",
    "cases.card.wagari.desc": "O Wagari Carbon é um ecossistema sustentável para certificação, rastreamento e comercialização de créditos de carbono com transparência distribuída.",
    "cases.card.urus.desc": "Urus Stack é uma infraestrutura de desenvolvimento moderna que automatiza o ciclo completo de spec, código e validação por múltiplos agentes de IA.",
    "cases.card.paraguay.desc": "O Portal Paraguai é uma plataforma altamente estável para faturamento eletrônico e conexões financeiras multi-moeda na América Latina.",
    "cases.tag.agro": "Agro Embedded Finance",
    "cases.card.agro.desc": "Ceres Trading implementa uma arquitetura de Agro Embedded Financing, integrando motores de scoring de satélite e clima com gateways de crédito e derivativos agrícolas.",

    "contact.title": "Comece Seu Projeto de Alta Tecnologia",
    "contact.desc": "Desenhe as especificações do seu sistema e implante uma squad especialista de engenheiros e agentes de IA dedicados.",
    "contact.form.name": "Nome / Empresa",
    "contact.form.email": "E-mail de contato",
    "contact.form.message": "Descreva o fluxo de processos ou pagamentos que deseja automatizar",
    "contact.form.submit": "Iniciar Conexão de Projeto",
    "contact.form.sending": "Estabelecendo túnel criptografado...",
    "contact.form.success": "Mensagem transmitida com sucesso! Nossa equipe técnica retornará a conexão de forma segura."
  },
  en: {
    "nav.home": "Home",
    "nav.methodology": "Methodology",
    "nav.services": "Specialties",
    "nav.cases": "Case Studies",
    "nav.contact": "Contact",
    
    "hero.subtitle": "EPYX SAS Uruguay",
    "hero.title": "Payment Platforms and Process Automation with Intensive AI",
    "hero.desc": "Conceived and engineered in Uruguay for the world. Highly specialized squads integrating virtual developers and seasoned architects under a Spec-First methodology.",
    "hero.cta.simulate": "Simulate AI Squad",
    "hero.cta.contact": "Talk to an Expert",
    
    "methodology.subtitle": "Core Methodology",
    "methodology.title": "Spec-First & Vibe Coding",
    "methodology.desc": "The next generation of software engineering is not about writing code blindly. We build systems by designing detailed specifications prepared for multiple autonomous agents to collaborate and build.",
    "methodology.step1.title": "1. Architecture (Spec-First)",
    "methodology.step1.desc": "We build comprehensive architecture blueprints. A detailed spec prevents AI hallucination and defines robust interfaces before coding starts.",
    "methodology.step2.title": "2. Virtual Agent Squads",
    "methodology.step2.desc": "We deploy dedicated development squads using virtual programmers like Antigravity, Claude Code, and Cursor working in parallel.",
    "methodology.step3.title": "3. Automated Validation",
    "methodology.step3.desc": "Autonomous validating agents run testing suites, enforce strict compiler types, and perform security audits before deployment.",

    "simulator.subtitle": "System Console",
    "simulator.title": "AI Squad Simulator",
    "simulator.label": "Select a project configuration to watch virtual agents cooperate:",
    "simulator.project.care": "Multi-Agent Customer Care",
    "simulator.project.docs": "Legal Document Classification",
    "simulator.project.payments": "Multi-Currency Payments & Pix",
    "simulator.project.tokenization": "Asset Tokenization & Crypto",
    "simulator.project.carbon": "Carbon Data & Renewable Energy",
    "simulator.project.agro": "Agro Embedded Financing",
    "simulator.terminal.tab.logs": "System Logs",
    "simulator.terminal.tab.chat": "Agent Conversation",
    "simulator.terminal.tab.code": "Generated Code",
    "simulator.btn.run": "Run Squad Simulation",
    "simulator.terminal.idle": "Console idle. Choose a configuration above and click 'Run Squad Simulation'...",

    "services.subtitle": "Specialties",
    "services.title": "High-Complexity Engineering",
    "services.card.care.title": "Multi-Agent Customer Care Systems",
    "services.card.care.desc": "Integrated support loops where dedicated AI agents analyze sentiment, categorize tickets, and resolve advanced client requests autonomously.",
    "services.card.docs.title": "Legal Classification & Clustering",
    "services.card.docs.desc": "NLP parsing engines classifying and clustering legally binding contracts. Semantic search, metadata extraction, and legal auditing.",
    "services.card.payments.title": "Multi-Currency & International Pix",
    "services.card.payments.desc": "High-throughput payment gateways built in Uruguay for global checkouts. Multi-currency wallets, instant conversions, and local clearing.",
    "services.card.tokenization.title": "Asset Tokenization & Capitalization",
    "services.card.tokenization.desc": "Real-world asset (RWA) tokenization and digital structuring for tech & fintech capitalization using secure smart contracts.",
    "services.card.credit.title": "Crypto-Collateralized Financing",
    "services.card.credit.desc": "Token-collateralized loan frameworks integrating Chainlink price feeds and automated liquidation triggers based on coverage ratios.",
    "services.card.apps.title": "Last-Mile Payment Applications",
    "services.card.apps.desc": "Modern mobile wallet architectures crafted with bank-grade security protocols, robust state management, and smooth UX.",
    "services.card.agro.title": "Agro Embedded Financing",
    "services.card.agro.desc": "Integration of credit gateways, satellite scoring, and multi-currency wallets for agricultural supply chains and digital CPR issuance.",
    "services.card.carbon.title": "Lead Generation & Carbon Emissions",
    "services.card.carbon.desc": "Data analysis, IoT emission monitoring, and structuring of renewable energy projects backed by crypto assets.",

    "cases.subtitle": "Conceived in Uruguay for the World",
    "cases.title": "Real Cases in Production",
    "cases.tag.fintech": "Fintech & Blockchain",
    "cases.tag.dev": "Developer Tooling",
    "cases.tag.sustainability": "Sustainability",
    "cases.card.wagari.desc": "Wagari Carbon is a green ledger platform engineered to audit, trace, and trade global carbon offsets with distributed, tamper-proof security.",
    "cases.card.urus.desc": "Urus Stack is an advanced developer tool suite automating specification-to-code pipelines across multi-agent virtual deployments.",
    "cases.card.paraguay.desc": "Portal Paraguai consolidates electronic invoicing and regional multi-currency clearing for high-performance enterprise operations.",
    "cases.tag.agro": "Agro Embedded Finance",
    "cases.card.agro.desc": "Ceres Trading implements an Agro Embedded Financing architecture, integrating satellite and climate scoring engines with credit gateways and agricultural derivatives.",

    "contact.title": "Initiate Next-Gen Engineering",
    "contact.desc": "Let's build your spec-first blueprint and deploy a dedicated squad of seasoned human architects and virtual developers.",
    "contact.form.name": "Name / Company",
    "contact.form.email": "Contact Email",
    "contact.form.message": "Describe the core transaction, database, or agent system you want to build",
    "contact.form.submit": "Submit Project Brief",
    "contact.form.sending": "Opening secure socket tunnel...",
    "contact.form.success": "Secure transmission complete. Our routing node will reply shortly via encrypted channels."
  }
};

let currentLanguage = 'es'; // default

// Simulator project mock data
const simulatorData = {
  care: {
    logs: [
      "STATUS: Initiating multi-agent customer care squad...",
      "SPECIFICATION: Load spec file 'spec/customer_care_api.json'...",
      "ARCHITECT: Human Architect finalized interface constraints for /api/support/ticket.",
      "DEPLOYING: Deploying virtual developers (Antigravity & Claude Code)...",
      "ANTIGRAVITY: Scaffolding API route handler with token parsing: [OK]",
      "CLAUDE CODE: Designing classification module using semantic embedding distance... [COMPILING]",
      "CLAUDE CODE: Routing logic compiled successfully.",
      "ANTIGRAVITY: Writing integration tests for sentiment scoring: [OK]",
      "VALIDATOR: Running unit tests... 12/12 successful.",
      "VALIDATOR: Analyzing security vulnerabilities: Zero issues found. Types strict.",
      "COMPLETED: API deployed to stage-env. Logs routed to dashboard.",
      "SYSTEM: Deployment finalized in 4200ms."
    ],
    chat: [
      { sender: "Architect", message: "We need an endpoint /api/support/ticket that extracts intent, classifies into 5 categories, and opens a websocket for real-time priority updates." },
      { sender: "Antigravity", message: "Understood. I'll setup the routing infrastructure in Express with JWT authentication, validation schemas, and rate limiters." },
      { sender: "Claude Code", message: "I will implement the classification controller. We'll use a local embedding vector model to avoid high latency. If sentiment is < -0.7, we tag as CRITICAL." },
      { sender: "Antigravity", message: "Nice addition, Claude. Let's make sure the validation middleware sanitizes all inputs against HTML injection. Writing code now." },
      { sender: "Claude Code", message: "Code looks solid. Let me add the unit tests for classification scoring to test the threshold boundary conditions." },
      { sender: "Validator", message: "All 12 tests passed successfully. Code coverage at 98%. Security verification: no unsafe code paths detected." }
    ],
    code: `// routes/ticket.js
import { Router } from 'express';
import { authenticate } from '../middleware/auth.js';
import { classifyTicket } from '../services/classifier.js';
import { sanitize } from '../utils/security.js';

const router = Router();

router.post('/ticket', authenticate, async (req, res) => {
  try {
    const rawContent = req.body.content;
    const content = sanitize(rawContent);
    
    // Classify ticket using local NLP agent pipeline
    const { category, sentiment, urgency } = await classifyTicket(content);
    
    const ticket = await req.db.Ticket.create({
      userId: req.user.id,
      content,
      category,
      sentiment,
      urgency: sentiment < -0.7 ? 'CRITICAL' : urgency,
      status: 'OPEN'
    });

    // Notify support live socket channel
    req.io.to('admin-support').emit('new_ticket', { id: ticket.id, urgency });

    return res.status(201).json({ success: true, ticket });
  } catch (error) {
    return res.status(500).json({ error: 'Failed to route ticket' });
  }
});

export default router;`
  },
  docs: {
    logs: [
      "STATUS: Initializing Legal Classification Engine...",
      "SPECIFICATION: Loading spec 'spec/legal_document_clusterer.json'...",
      "ARCHITECT: Human Architect defined structure for legal TF-IDF matrix parsing.",
      "DEPLOYING: Deploying virtual developers (Antigravity & Cursor)...",
      "CURSOR: Scaffolding python vector classification script `document_vectorizer.py`...",
      "ANTIGRAVITY: Implementing SHA-256 anchoring and validation in `doc_signature.js`...",
      "CURSOR: Vector similarity indexing configured. Testing on 150 mock legal templates: [COMPILING]",
      "CURSOR: Clustering accuracy reached 98.4%. Standard deviation within acceptable bounds.",
      "ANTIGRAVITY: Mock API database integrations set up and connected: [OK]",
      "VALIDATOR: Running unit tests... 18/18 tests passed.",
      "COMPLETED: Legal Classifier pipeline is active. Live on production branch.",
      "SYSTEM: Deployment finalized in 4750ms."
    ],
    chat: [
      { sender: "Architect", message: "Goal: Read unstructured contract PDFs, extract key legal clauses (liability, term, venue), calculate similarity index, and record document hashes on a ledger." },
      { sender: "Cursor", message: "I'll write the Python backend processing using `pdfplumber` and `scikit-learn` to extract text and generate spatial TF-IDF values." },
      { sender: "Antigravity", message: "I will implement the Node.js API server that anchors the metadata and generates verifiable hashes on our ledger backend. I'll make it highly secure." },
      { sender: "Cursor", message: "Great. I'm adding cosine similarity calculations so we can cluster contracts by company template patterns automatically." },
      { sender: "Antigravity", message: "I have added the cryptographic signature check logic and connected the webhook so python results are processed asynchronously." },
      { sender: "Validator", message: "System test verification complete. Vector distance bounds are strictly validated. Output formatting meets legal standards. Cryptographic hashes matching." }
    ],
    code: `# services/document_vectorizer.py
import numpy as np
from sklearn.feature_extraction.text import TfidfVectorizer
from sklearn.metrics.pairwise import cosine_similarity

class LegalClusterer:
    def __init__(self, reference_templates):
        self.vectorizer = TfidfVectorizer(stop_words='english', ngram_range=(1, 3))
        # Pre-fit templates
        self.template_vectors = self.vectorizer.fit_transform(reference_templates)

    def analyze_document(self, new_doc_text):
        new_vector = self.vectorizer.transform([new_doc_text])
        similarities = cosine_similarity(new_vector, self.template_vectors)[0]
        
        best_match_idx = np.argmax(similarities)
        confidence = similarities[best_match_idx]
        
        # Determine classification
        classification = "UNKNOWN"
        if confidence > 0.85:
            classification = f"TEMPLATE_MATCH_{best_match_idx}"
        elif confidence > 0.50:
            classification = "CLAUSE_VARIATION"
            
        return {
            "classification": classification,
            "confidence": float(confidence),
            "similarity_matrix": similarities.tolist()
        }`
  },
  payments: {
    logs: [
      "STATUS: Booting Multi-Currency & Pix Core Ledger...",
      "SPECIFICATION: Loading spec 'spec/pix_multi_currency_exchange.json'...",
      "ARCHITECT: Human Architect defined webhook payload signature and currency state locks.",
      "DEPLOYING: Deploying virtual developers (Antigravity & Claude Code)...",
      "ANTIGRAVITY: Scaffolding API controller for international Pix webhook in Node.js...",
      "CLAUDE CODE: Creating currency calculation module `exchange_rate_service.js` with lock validation...",
      "ANTIGRAVITY: Implementing anti-double-spend database transaction locks: [OK]",
      "CLAUDE CODE: Exchange caching setup (updated every 60s via external bank feed): [OK]",
      "VALIDATOR: Running mock payments. Attempting double spend simulation... [OK - BLOCKED]",
      "VALIDATOR: Ledger balancing checks out (net-zero matching). 24 tests passed.",
      "COMPLETED: Ledger deployed. Real-time multi-currency settlement active.",
      "SYSTEM: Deployment finalized in 5100ms."
    ],
    chat: [
      { sender: "Architect", message: "Requirement: Build a secure international Pix webhook. Once received, convert the amount from Brazilian Real (BRL) to Uruguayan Peso (UYU) or USD, debiting the reserve wallet instantly." },
      { sender: "Antigravity", message: "I will start writing the webhook validation. Idempotency is crucial here. We need a redis lock on the `endToEndId` of Pix to prevent double execution." },
      { sender: "Claude Code", message: "I'll create the currency exchange manager. It will retrieve exchange rates, apply the EPYX margin, and execute ledger updates with precise decimal arithmetic to avoid floating point errors." },
      { sender: "Antigravity", message: "Good point on decimals. Let's use BigInt or integer representation (cents/centavos) in the database." },
      { sender: "Claude Code", message: "Database schemas updated. I'm coding the conversion lock logic so that users get a guaranteed rate for 10 seconds during the transaction." },
      { sender: "Validator", message: "Ledger transaction checks out. Testing concurrent updates: race conditions resolved. Double spend attempt correctly blocked with HTTP 409." }
    ],
    code: `// services/pix-settlement.js
import DB from '../models/db.js';
import { calculateExchange } from './exchange-rates.js';

export async function processPixPayment({ endToEndId, amountBrl, targetCurrency, walletId }) {
  // Use transactional lock to ensure idempotency and atomic balances
  return await DB.sequelize.transaction(async (t) => {
    // 1. Check if Pix is already processed
    const existing = await DB.Transaction.findOne({
      where: { pixId: endToEndId },
      transaction: t,
      lock: true
    });
    
    if (existing) {
      throw new Error('DUPLICATE_TRANSACTION'); // Handled as HTTP 409
    }

    // 2. Fetch destination wallet
    const wallet = await DB.Wallet.findOne({
      where: { id: walletId },
      transaction: t,
      lock: true
    });

    // 3. Perform conversion (safe integer cents math)
    const { targetAmount, rate } = await calculateExchange(amountBrl, 'BRL', targetCurrency);

    // 4. Update balances and create transaction logs
    wallet.balance += targetAmount;
    await wallet.save({ transaction: t });

    const tx = await DB.Transaction.create({
      pixId: endToEndId,
      walletId,
      amountSource: amountBrl,
      currencySource: 'BRL',
      amountTarget: targetAmount,
      currencyTarget: targetCurrency,
      exchangeRate: rate,
      status: 'SETTLED'
    }, { transaction: t });

    return { tx, newBalance: wallet.balance };
  });
}`
  },
  tokenization: {
    logs: [
      "STATUS: Initiating Asset Tokenization & Collateral Ledger...",
      "SPECIFICATION: Loading spec 'spec/tokenization_vault.json'...",
      "ARCHITECT: Human Architect defined parameters for RWA fractionalization & health ratios.",
      "DEPLOYING: Deploying virtual developers (Antigravity & Claude Code)...",
      "ANTIGRAVITY: Scaffolding ERC-20 / ERC-1155 smart contracts for Real World Assets: [OK]",
      "CLAUDE CODE: Designing Oracle integration with Chainlink pricing protocols... [COMPILING]",
      "ANTIGRAVITY: Compiling solidity vault contracts... [OK - GAS MINIMIZED]",
      "CLAUDE CODE: Implementing liquidation health factor thresholds: [OK]",
      "VALIDATOR: Running simulation on testnet. Collateral drops to 125%... [OK - LIQUIDATION TRIGGERED]",
      "VALIDATOR: Reentrancy audit: PASSED. Checked with Slither and Mythril compiler tools.",
      "COMPLETED: Tokenization contracts active. Smart vault listening to Oracle pricing node.",
      "SYSTEM: Deployment finalized in 5800ms."
    ],
    chat: [
      { sender: "Architect", message: "Goal: Build a fractional tokenization contract that records asset ownership. Users can deposit these RWA tokens as collateral and borrow stablecoins." },
      { sender: "Antigravity", message: "Understood. I will write the `RWACollateralVault.sol` contract using OpenZeppelin standards. Security is critical due to collateral lock assets." },
      { sender: "Claude Code", message: "I'll handle the oracle price feed integration. We'll pull active asset prices and calculate the current health factor in real-time. If it dips below 130%, we trigger liquidation." },
      { sender: "Antigravity", message: "Perfect. I'm adding `ReentrancyGuard` to the withdraw and borrow endpoints, ensuring we prevent concurrent state re-entry vector attacks." },
      { sender: "Claude Code", message: "Tested the conversion formulas. Safe math is built-in in Solidity 0.8.20. Let's write verification tests for the oracle data validation." },
      { sender: "Validator", message: "Compiled successfully. Checked against common smart contract vulnerability vectors. Liquidation thresholds triggered properly on mock testing environment." }
    ],
    code: `// contracts/RWACollateralVault.sol
// SPDX-License-Identifier: MIT
pragma solidity ^0.8.20;

import "@openzeppelin/contracts/token/ERC20/IERC20.sol";
import "@openzeppelin/contracts/security/ReentrancyGuard.sol";
import "@openzeppelin/contracts/access/Ownable.sol";

interface IPriceOracle {
    function getLatestPrice(address asset) external view returns (uint256);
}

contract RWACollateralVault is ReentrancyGuard, Ownable {
    IERC20 public stablecoin;
    IPriceOracle public priceOracle;
    
    uint256 public constant MIN_COLLATERAL_RATIO = 130; // 130%
    
    struct Vault {
        uint256 collateralAmount;
        uint256 borrowedAmount;
    }
    
    mapping(address => Vault) public vaults;
    
    event CollateralDeposited(address indexed user, uint256 amount);
    event TokensBorrowed(address indexed user, uint256 amount);
    event LiquidationTriggered(address indexed user, uint256 amountLiquidated);

    constructor(address _stablecoin, address _oracle) Ownable(msg.sender) {
        stablecoin = IERC20(_stablecoin);
        priceOracle = IPriceOracle(_oracle);
    }

    function depositCollateral(uint256 amount) external nonReentrant {
        // ... deposit implementation details
        emit CollateralDeposited(msg.sender, amount);
    }

    function checkCollateralization(address user) public view returns (uint256) {
        Vault memory vault = vaults[user];
        if (vault.borrowedAmount == 0) return type(uint256).max;
        
        uint256 assetPrice = priceOracle.getLatestPrice(address(this));
        uint256 collateralValue = (vault.collateralAmount * assetPrice) / 1e18;
        
        return (collateralValue * 100) / vault.borrowedAmount;
    }
}`
  },
  carbon: {
    logs: [
      "STATUS: Initiating Green Data Engine (Wagari Carbon ref)...",
      "SPECIFICATION: Loading spec 'spec/carbon_lead_identifier.json'...",
      "ARCHITECT: Human Architect defined parameters for emission thresholds & renewable financing.",
      "DEPLOYING: Deploying virtual developers (Antigravity & Cursor)...",
      "ANTIGRAVITY: Scaffolding Data Visualization dashboard for IoT emission tracking: [OK]",
      "CURSOR: Designing ML clustering model to identify high-emission leads... [COMPILING]",
      "ANTIGRAVITY: Linking crypto-collateral engine for renewable energy project financing: [OK]",
      "CURSOR: Backtesting lead generation against Wagari Carbon historical datasets: [OK]",
      "VALIDATOR: Running simulation. 450 high-emission leads identified for offset sales.",
      "VALIDATOR: Smart contract audit for collateralized financing: PASSED.",
      "COMPLETED: Carbon offset & Renewable financing platform active.",
      "SYSTEM: Deployment finalized in 5100ms."
    ],
    chat: [
      { sender: "Architect", message: "Goal: Build a data visualization tool that identifies high-emission leads. We can offer them Wagari Carbon offsets or renewable energy projects financed via crypto collateral." },
      { sender: "Antigravity", message: "Understood. I will build the real-time visualization dashboard and connect the crypto-collateral engine to issue renewable energy bonds." },
      { sender: "Cursor", message: "I'll handle the data processing. I will train a clustering model on historical Wagari Carbon data to pinpoint companies that exceed emission thresholds." },
      { sender: "Antigravity", message: "Perfect. When a lead is identified, the system will automatically propose a smart contract for offset purchase or collateralized financing." },
      { sender: "Cursor", message: "The ML model is compiled. I've integrated the crypto guarantee parameters so they adjust based on the client's risk profile." },
      { sender: "Validator", message: "Integration tested. Data pipelines are secure. The smart contracts for renewable financing passed the security audit with 100% coverage." }
    ],
    code: `// core/carbon_finance.js
import { MLCluster } from '@wagari/ml-core';
import { CryptoCollateralEngine } from '../finance/collateral.js';
import { Tokenizer } from '../blockchain/tokenizer.js';

export class CarbonLeadEngine {
  constructor(dataset) {
    this.model = new MLCluster({ threshold: 'HIGH_EMISSION' });
    this.dataset = dataset;
  }

  async identifyLeads() {
    const predictions = await this.model.predict(this.dataset);
    return predictions.filter(lead => lead.emissionScore > 85);
  }

  async proposeFinancing(leadId, energyProject) {
    const collateralEngine = new CryptoCollateralEngine();
    const riskProfile = await collateralEngine.assessRisk(leadId);
    
    // Propose Renewable Energy project backed by crypto
    const bond = await Tokenizer.issueRenewableBond({
      leadId,
      project: energyProject,
      collateralRatio: riskProfile.requiredRatio,
      asset: 'WAGARI_TOKEN'
    });
    
    return bond;
  }
}`
  },
  agro: {
    logs: [
      "STATUS: Initiating Agro Embedded Finance Engine (Ceres Trading ref)...",
      "SPECIFICATION: Loading spec 'spec/agro_financing_cpr.json'...",
      "ARCHITECT: Defined constraints for IoT satellite scoring and Multi-Currency Wallet.",
      "DEPLOYING: Deploying virtual developers (Antigravity & Claude Code)...",
      "ANTIGRAVITY: Scaffolding Multi-Currency Wallet with real-time FX tracking: [OK]",
      "CLAUDE CODE: Designing NDVI Satellite Scoring model for credit assessment... [COMPILING]",
      "ANTIGRAVITY: Linking Digital CPR (Cédula de Produto Rural) smart contracts: [OK]",
      "CLAUDE CODE: Backtesting risk profiles with Serasa + Receita Federal APIs: [OK]",
      "VALIDATOR: Running simulation. 1500 farm producers scored. Credit limits generated.",
      "VALIDATOR: AgroShield insurance parametric triggers validated.",
      "COMPLETED: Agro Embedded Finance portal active.",
      "SYSTEM: Deployment finalized in 4800ms."
    ],
    chat: [
      { sender: "Architect", message: "Goal: Build an Agro Embedded Finance platform (like Ceres Trading). We need a multi-currency wallet, satellite credit scoring, and digital CPR issuance." },
      { sender: "Antigravity", message: "I'll handle the Multi-Currency wallet. It will process USD, EUR, CNY, and BRL with real-time FX spreads." },
      { sender: "Claude Code", message: "For the credit engine, I'll combine Serasa scoring with satellite NDVI crop data. This will dynamically adjust the credit limit for producers." },
      { sender: "Antigravity", message: "Excellent. Let's also implement the Barter calculator so producers can trade urea for future soybeans using digital CPRs." },
      { sender: "Claude Code", message: "Barter endpoints are live. I'm adding AgroShield integration to issue insurance policies automatically based on vessel IoT checkpoints." },
      { sender: "Validator", message: "All systems go. The FX ticker is streaming, the CPRs are cryptographically signed, and the credit engine is correctly evaluating risk." }
    ],
    code: `// core/agro_credit_engine.js
import { SatelliteScoring } from '@ceres/ndvi-score';
import { CPRGenerator } from '../blockchain/cpr.js';
import { FXWallet } from '../finance/fx-wallet.js';

export class AgroEmbeddedFinance {
  constructor(producerId) {
    this.producerId = producerId;
    this.scoring = new SatelliteScoring();
    this.wallet = new FXWallet();
  }

  async calculateCreditLimit() {
    const ndviScore = await this.scoring.analyzeCropYield(this.producerId);
    const serasaScore = await this.fetchSerasaScore(this.producerId);
    
    let baseLimit = 500000;
    if (ndviScore > 85 && serasaScore > 750) {
      baseLimit += 2000000; // Premium limit
    }
    return baseLimit;
  }

  async issueBarterCPR(ureaVolume, grainType) {
    const equivalentGrain = await this.wallet.calculateBarter(ureaVolume, grainType);
    
    // Issue Digital CPR (Cédula de Produto Rural)
    const cpr = await CPRGenerator.issueDigitalCPR({
      producerId: this.producerId,
      grainType,
      volume: equivalentGrain,
      dueDate: '2027-03-01'
    });
    
    return cpr;
  }
}`
  }
};
// Set active language
function setLanguage(lang) {
  if (!translations[lang]) return;
  currentLanguage = lang;
  document.documentElement.lang = lang;
  
  // Update translation text across DOM elements
  document.querySelectorAll('[data-i18n]').forEach(element => {
    const key = element.getAttribute('data-i18n');
    const translation = translations[lang][key];
    
    if (translation) {
      if (element.tagName === 'INPUT' || element.tagName === 'TEXTAREA') {
        element.placeholder = translation;
      } else {
        element.innerHTML = translation;
      }
    }
  });

  // Update active translation button styling
  document.querySelectorAll('.lang-btn').forEach(btn => {
    if (btn.getAttribute('data-lang') === lang) {
      btn.classList.add('active');
    } else {
      btn.classList.remove('active');
    }
  });

  localStorage.setItem('epyx_lang', lang);
}

// Router - Dynamic page navigation simulation
function handleRouting() {
  const hash = window.location.hash || '#home';
  const targetId = hash.replace('#', '');
  const targetSection = document.getElementById(targetId);

  // Update navbar styling
  document.querySelectorAll('.nav-link').forEach(link => {
    if (link.getAttribute('href') === hash) {
      link.classList.add('active');
    } else {
      link.classList.remove('active');
    }
  });

  if (targetSection) {
    targetSection.scrollIntoView({ behavior: 'smooth', block: 'start' });
  }
}

// Simulator UI management
let simIntervals = [];
function clearSimulation() {
  simIntervals.forEach(clearInterval);
  simIntervals = [];
  
  // Clear outputs
  document.getElementById('sim-logs').innerHTML = '';
  document.getElementById('sim-chat').innerHTML = '';
  document.getElementById('sim-code').textContent = '';
}

function runSimulation(projectKey) {
  clearSimulation();
  
  const data = simulatorData[projectKey];
  if (!data) return;

  const logsContainer = document.getElementById('sim-logs');
  const chatContainer = document.getElementById('sim-chat');
  const codeContainer = document.getElementById('sim-code');

  // Change simulation state
  document.querySelector('.simulator-workspace').classList.add('running');
  document.querySelector('.sim-btn-run').disabled = true;

  // Let's create timelines for typing logs, chat dialogue, and writing code.
  
  // 1. Logs timeline
  let logIdx = 0;
  function printNextLog() {
    if (logIdx < data.logs.length) {
      const line = document.createElement('div');
      line.className = 'log-line';
      
      // Syntax highlighting for logs
      let html = data.logs[logIdx];
      html = html.replace(/\[OK\]/g, '<span class="log-ok">[OK]</span>');
      html = html.replace(/\[COMPILING\]/g, '<span class="log-warning">[COMPILING]</span>');
      html = html.replace(/(STATUS:|SPECIFICATION:|ARCHITECT:|DEPLOYING:|ANTIGRAVITY:|CLAUDE CODE:|CURSOR:|VALIDATOR:|COMPLETED:|SYSTEM:)/g, '<span class="log-tag">$1</span>');
      
      line.innerHTML = html;
      logsContainer.appendChild(line);
      logsContainer.scrollTop = logsContainer.scrollHeight;
      
      logIdx++;
      const delay = Math.random() * 300 + 150;
      const t = setTimeout(printNextLog, delay);
      simIntervals.push(t);
    } else {
      // Completed, re-enable button
      document.querySelector('.sim-btn-run').disabled = false;
      document.querySelector('.simulator-workspace').classList.remove('running');
    }
  }

  // 2. Chat timeline
  let chatIdx = 0;
  function printNextChat() {
    if (chatIdx < data.chat.length) {
      const chatItem = data.chat[chatIdx];
      const bubble = document.createElement('div');
      bubble.className = `chat-bubble ${chatItem.sender.toLowerCase().replace(' ', '-')}`;
      
      let avatar = '🤖';
      if (chatItem.sender.includes('Architect')) avatar = '👨‍💻';
      if (chatItem.sender.includes('Antigravity')) avatar = '⚡';
      if (chatItem.sender.includes('Claude')) avatar = '🔮';
      if (chatItem.sender.includes('Cursor')) avatar = '🛠️';
      if (chatItem.sender.includes('Validator')) avatar = '🛡️';

      bubble.innerHTML = `
        <div class="chat-meta">
          <span class="chat-avatar">${avatar}</span>
          <span class="chat-sender">${chatItem.sender}</span>
        </div>
        <div class="chat-message">${chatItem.message}</div>
      `;
      chatContainer.appendChild(bubble);
      chatContainer.scrollTop = chatContainer.scrollHeight;

      chatIdx++;
      const delay = Math.random() * 1000 + 1200;
      const t = setTimeout(printNextChat, delay);
      simIntervals.push(t);
    }
  }

  // 3. Code Typing effect
  let codeCharIdx = 0;
  const codeText = data.code;
  function typeCode() {
    if (codeCharIdx < codeText.length) {
      // Type 4 characters at a time for coding speed look
      const chunk = codeText.substring(codeCharIdx, codeCharIdx + 4);
      codeContainer.textContent += chunk;
      
      // Simple custom highlight inside compiler if needed or just keep raw code
      codeCharIdx += 4;
      
      // Keep scroll down
      const block = codeContainer.parentElement;
      block.scrollTop = block.scrollHeight;

      const delay = Math.random() * 30 + 15;
      const t = setTimeout(typeCode, delay);
      simIntervals.push(t);
    }
  }

  // Kick off sequences
  printNextLog();
  // Delay chat start slightly
  const chatTimer = setTimeout(printNextChat, 400);
  simIntervals.push(chatTimer);
  // Delay code start to simulate agent coding later
  const codeTimer = setTimeout(typeCode, 1500);
  simIntervals.push(codeTimer);
}

// ============================================================
// Zoho CRM Integration — Real Lead Creation via OAuth 2.0
// ============================================================

// Step 1: Exchange refresh_token for a fresh access_token
async function getZohoAccessToken() {
  const params = new URLSearchParams({
    refresh_token: ZOHO_CONFIG.refreshToken,
    client_id:     ZOHO_CONFIG.clientId,
    client_secret: ZOHO_CONFIG.clientSecret,
    grant_type:    'refresh_token',
  });

  const res = await fetch(`${ZOHO_CONFIG.accountsUrl}/oauth/v2/token`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/x-www-form-urlencoded' },
    body: params.toString(),
  });

  if (!res.ok) throw new Error(`Token exchange failed: ${res.status}`);
  const data = await res.json();
  if (!data.access_token) throw new Error(data.error || 'No access_token returned');
  return data.access_token;
}

// Step 2: Create Lead in Zoho CRM
async function createZohoLead({ name, email, message }) {
  const accessToken = await getZohoAccessToken();

  // Split name into First / Last for Zoho CRM fields
  const nameParts = name.trim().split(' ');
  const lastName  = nameParts.length > 1 ? nameParts.slice(1).join(' ') : nameParts[0];
  const firstName = nameParts.length > 1 ? nameParts[0] : '';

  const leadPayload = {
    data: [
      {
        Last_Name:   lastName,
        First_Name:  firstName,
        Email:       email,
        Description: message,
        Lead_Source: 'Web Site',
        Company:     name, // Company field = full name/company from form
      },
    ],
  };

  const res = await fetch(`${ZOHO_CONFIG.apiDomain}/crm/v7/Leads`, {
    method:  'POST',
    headers: {
      'Authorization': `Zoho-oauthtoken ${accessToken}`,
      'Content-Type':  'application/json',
    },
    body: JSON.stringify(leadPayload),
  });

  if (!res.ok) throw new Error(`CRM API error: ${res.status}`);
  const result = await res.json();

  // Check for CRM-level errors
  const record = result.data?.[0];
  if (record && record.status === 'error') {
    throw new Error(record.message || 'CRM rejected the lead');
  }

  return result;
}

// Contact Form — real Zoho CRM submission handler
function setupContactForm() {
  const form = document.getElementById('epyx-contact-form');
  const responseDiv = document.getElementById('form-response');
  if (!form) return;

  form.addEventListener('submit', async (e) => {
    e.preventDefault();

    const submitBtn  = form.querySelector('button[type="submit"]');
    const originalHTML = submitBtn.innerHTML;

    const name    = document.getElementById('form-name').value.trim();
    const email   = document.getElementById('form-email').value.trim();
    const message = document.getElementById('form-message').value.trim();

    // Disable form while submitting
    form.querySelectorAll('input, textarea, button').forEach(el => el.disabled = true);
    submitBtn.innerHTML = `<span class="spinner"></span> <span data-i18n="contact.form.sending">${translations[currentLanguage]['contact.form.sending']}</span>`;
    responseDiv.innerHTML = '';

    // Animate terminal handshake lines while waiting
    const terminalLines = [
      '&gt; OPENING SECURE TUNNEL [AES-256]...',
      '&gt; AUTHENTICATING WITH CRM NODE...',
      '&gt; ENCRYPTING PAYLOAD...',
      '&gt; TRANSMITTING PROJECT DATA...',
    ];
    let lineIdx = 0;
    responseDiv.className = 'form-response-success';
    responseDiv.innerHTML = `<div class="terminal-success" id="terminal-anim"></div>`;
    const terminalEl = document.getElementById('terminal-anim');

    const lineTimer = setInterval(() => {
      if (lineIdx < terminalLines.length) {
        const p = document.createElement('p');
        p.className = 'blink-fast';
        p.innerHTML = terminalLines[lineIdx++];
        terminalEl.appendChild(p);
      } else {
        clearInterval(lineTimer);
      }
    }, 500);

    try {
      await createZohoLead({ name, email, message });

      clearInterval(lineTimer);

      // Success state
      terminalEl.innerHTML = `
        <p class="blink-fast">&gt; SECURE TUNNEL OPENED [AES-256]</p>
        <p>&gt; LEAD REGISTERED IN CRM [OK]</p>
        <p>&gt; METADATA ENCRYPTED &amp; LOGGED</p>
        <p class="success-text" data-i18n="contact.form.success">${translations[currentLanguage]['contact.form.success']}</p>
      `;

      form.reset();
      responseDiv.scrollIntoView({ behavior: 'smooth', block: 'nearest' });

    } catch (err) {
      clearInterval(lineTimer);
      console.error('Zoho CRM submission error:', err);

      responseDiv.className = 'form-response-error';
      responseDiv.innerHTML = `
        <div class="terminal-success">
          <p style="color: #f87171;">&gt; ERROR: Could not transmit data.</p>
          <p style="color: #f87171;">&gt; ${err.message}</p>
          <p style="color: var(--text-muted); margin-top: 8px;">Please try again or contact us directly.</p>
        </div>
      `;
    } finally {
      // Re-enable form
      form.querySelectorAll('input, textarea, button').forEach(el => el.disabled = false);
      submitBtn.innerHTML = originalHTML;
    }
  });
}

// Initialise features
document.addEventListener('DOMContentLoaded', () => {
  // 1. Languages setup
  const savedLang = localStorage.getItem('epyx_lang') || 'es';
  setLanguage(savedLang);

  document.querySelectorAll('.lang-btn').forEach(btn => {
    btn.addEventListener('click', () => {
      const selected = btn.getAttribute('data-lang');
      setLanguage(selected);
    });
  });

  // 2. Hash Routing
  window.addEventListener('hashchange', handleRouting);
  // Run on load
  if (window.location.hash) {
    handleRouting();
  }

  // 3. Navigation link transitions
  document.querySelectorAll('.nav-link').forEach(link => {
    link.addEventListener('click', (e) => {
      e.preventDefault();
      const href = link.getAttribute('href');
      window.location.hash = href;
      handleRouting();
    });
  });

  // 4. Hero section CTAs linkage
  const simCta = document.querySelector('.hero-btn-simulate');
  if (simCta) {
    simCta.addEventListener('click', (e) => {
      e.preventDefault();
      window.location.hash = '#simulator';
      handleRouting();
    });
  }
  const contactCta = document.querySelector('.hero-btn-contact');
  if (contactCta) {
    contactCta.addEventListener('click', (e) => {
      e.preventDefault();
      window.location.hash = '#contact';
      handleRouting();
    });
  }

  // 5. Simulator Setup
  const projectBtns = document.querySelectorAll('.project-btn');
  let selectedProject = 'payments'; // Default starting project selection

  projectBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      projectBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      selectedProject = btn.getAttribute('data-project');
      
      clearSimulation();
      
      // Update preview idle message
      document.getElementById('sim-logs').innerHTML = `<div class="idle-text">${translations[currentLanguage]["simulator.terminal.idle"]}</div>`;
      document.getElementById('sim-chat').innerHTML = `<div class="idle-text">${translations[currentLanguage]["simulator.terminal.idle"]}</div>`;
      document.getElementById('sim-code').textContent = translations[currentLanguage]["simulator.terminal.idle"];
    });
  });

  const runBtn = document.querySelector('.sim-btn-run');
  if (runBtn) {
    runBtn.addEventListener('click', () => {
      runSimulation(selectedProject);
    });
  }

  // Select tabs in terminal
  const tabBtns = document.querySelectorAll('.tab-btn');
  tabBtns.forEach(btn => {
    btn.addEventListener('click', () => {
      tabBtns.forEach(b => b.classList.remove('active'));
      btn.classList.add('active');
      
      const targetPanel = btn.getAttribute('data-tab');
      document.querySelectorAll('.terminal-panel').forEach(panel => {
        panel.classList.remove('active');
      });
      document.getElementById(`panel-${targetPanel}`).classList.add('active');
    });
  });

  // 6. Intersection Observer for Fade-in effects
  const fadeElements = document.querySelectorAll('.fade-in-section');
  const observer = new IntersectionObserver((entries) => {
    entries.forEach(entry => {
      if (entry.isIntersecting) {
        entry.target.classList.add('is-visible');
        observer.unobserve(entry.target);
      }
    });
  }, {
    threshold: 0.15
  });

  fadeElements.forEach(el => observer.observe(el));

  // 7. Contact form validation
  setupContactForm();
});
